# NEXUS AGENTS DOCUMENT

## ExecPlans

When writing complex features or significant refactors, use an ExecPlan (as described in `.docs/PLANS.md`) from design to implementation. Write new plans to `.docs/exec/`. If inside Plan Mode, create the plan in a multiline markdown block, and write it after initiating implementation, so you can use the plan to guide your implementation and refer back to it as needed. If outside Plan Mode, you can write the plan directly and refer to it as needed.

## Rule

Always prefix shell commands with `rtk`.

Examples:

```bash
rtk git status
rtk cargo test
rtk npm run build
rtk pytest -q
rtk proxy <cmd>     # Run raw command without filtering
```

## RTK Verification

```bash
rtk --version
rtk gain
which rtk
```

## Development Details

Whenever new updates are made, this file (`AGENTS.md`) should be updated with any surprising files not apparent from the codebase that could benefit other developers. Focus on the why and when it could be useful.

- `web/apps/client/package.json` is the first file to check when the Next.js dev UI works on the host machine but not over LAN, because the `dev` script now explicitly uses `next dev --hostname 0.0.0.0` so phones/tablets on the same network can hit `<host-ip>:3000` while `localhost:3000` still works on the dev machine.
- `web/apps/client/lib/api-base-url.ts` and `web/apps/client/next.config.ts` are the first files to check when phone/tablet login fails with `NetworkError when attempting to fetch resource`, because local client API calls now fall back to a same-origin `"/__relay_api"` rewrite (to `http://localhost:4000`) whenever `NEXT_PUBLIC_API_URL` is missing or loopback-only on a non-loopback device.
- `web/apps/client/next.config.ts` is also the first file to check when LAN login works but HLS playback stays at `0:00` with no obvious app errors, because rewritten HLS playlists can emit absolute `"/stream/..."` segment URLs and the client host must proxy those paths to the API (not just `"/__relay_api/*"`).
- The repository root `.docs` path is a directory again. Write new ExecPlans under `.docs/exec/`; if `.docs/PLANS.md` is still absent, follow the nearby ExecPlans in `.docs/exec/` or `docs/relay-implementation-plan.md` for structure until a dedicated template is restored.
- `web/docker-compose.yml` is the Dokploy entrypoint for the web workspace, while `web/deploy/docker-compose.yml` stays dev-only. Check `web/deploy/dokploy/` first when Dokploy builds or startup healthchecks fail, and remember that `NEXT_PUBLIC_API_URL` is baked into the client image at build time, so changing the public API domain requires rebuilding the `client` service. Dokploy should route the public web app to `client` on port `3009`.
- `web/deploy/docker-compose.yml`, `web/deploy/.env.example`, and `web/apps/worker/src/index.ts` now align local Redis on port `6380`. Check those three first when a host-run worker still tries `localhost:6379` or when Docker-backed local services can see Redis internally but host processes cannot, because the local dev stack now binds Redis on `6380` and the worker's direct-run fallback matches that host port.
- `web/packages/contracts/src/index.ts` is the first file to check when the web client redesign needs richer page payloads, because the Next.js routes and Fastify API share their screen-facing schemas there and progress-aware changes to Library, Detail, Watch, Settings, or Providers usually start as contract additions before the page components can move.
- `web/packages/contracts/src/catalog.ts`, `web/packages/contracts/src/playback.ts`, and `web/packages/contracts/src/worker.ts` are now the first files to check when canonical catalog query params, NDJSON search stream events, watch-context query shapes, or worker queue contracts drift, because those shared schemas replaced the old single-file contract hub and the API, client, tests, and worker now all validate against those module-local exports.
- `web/packages/providers/src/provider-definitions.ts` is now the first file to check when provider base URLs, browser timeout policy, API timeout policy, playback cache TTLs, or browser context reuse rules feel inconsistent, because `@relay/providers`, `web/apps/browser`, and `web/apps/api` now read those runtime decisions from one shared definition map instead of carrying separate hardcoded overrides.
- `web/apps/api/src/config.ts`, `web/packages/providers/src/index.ts`, and `web/packages/providers/src/providers/xtream.ts` are the first files to check when Xtream disappears from the Sources list or never registers at startup, because Relay only instantiates the TMDB-backed Xtream provider when `TMDB_API_KEY` is present and the provider package itself does not read env directly.
- `web/apps/api/src/services/index.ts`, `web/apps/api/src/routes/catalog.ts`, `web/apps/api/src/services/catalog-service.ts`, and `web/apps/client/app/(dashboard)/discover/page.tsx` are the first files to check when Discover search progress counters look frozen or jump backwards, because `buildApi()` now wires thin route modules through a service container, `/catalog/search/stream` emits the NDJSON `start/progress/done` events in `routes/catalog.ts`, and provider aggregation plus last-search caching live in `catalog-service.ts`.
- `web/apps/api/src/streaming/proxy.ts` and `web/apps/api/src/streaming/compatibility-mp4.ts` are now the first files to check when HLS/DASH rewrite behavior, proxied fragment content types, forwarded playback headers, byte-range handling, or compatibility MP4 generation regress, because those stream-manipulation helpers were pulled out of `web/apps/api/src/app.ts` and now carry most of the proxy/manifest logic directly.
- `web/apps/api/src/services/catalog-service.ts`, `web/apps/api/src/services/library-service.ts`, `web/apps/api/src/services/history-service.ts`, and `web/apps/api/src/repositories/library-repository.ts` are the first files to check when Resume buttons, Continue Watching rows, grouped history, or watch-side episode state look stale, because the old API monolith’s view-model and progress-bookkeeping work was split across focused services and repository reads.
- `web/apps/client/lib/routes.ts`, `web/apps/client/lib/search-stream.ts`, `web/apps/client/lib/provider-links.ts`, `web/apps/client/components/cover-image.tsx`, `web/apps/client/hooks/use-provider-settings.ts`, and `web/apps/client/components/provider-settings-panel.tsx` are now the first files to check when slash-containing IDs break navigation, catalog/watch API paths drift, provider external links go stale, search stream parsing breaks, cover proxies regress, or provider controls diverge between settings surfaces, because those concerns were moved out of individual pages into shared client helpers/components.
- `web/apps/api/src/routes/playback.ts`, `web/apps/api/src/streaming/proxy.ts`, and `web/apps/api/src/services/playback-service.ts` are the first files to check when AnimePahe playback reaches Relay but Firefox still aborts after the manifest loads, because proxy normalization, manifest rewriting, and stale-session reuse rules were split across those files and `vault-*.owocdn.top` HLS media segments still arrive disguised as `.jpg` paths with image-like upstream content types.
- `web/apps/client/components/video-player.tsx`, `web/apps/api/src/routes/playback.ts`, and `web/apps/api/src/streaming/compatibility-mp4.ts` are the next files to check when AnimePahe still fails specifically in Firefox after the manifest, key, and first segment all load, because the stream audio is implicit HE-AACv2 inside MPEG-TS and `hls.js` derives an `mp4a.40.1` init segment that Firefox rejects or decodes with an AAC cookie error. Relay now works around that with a Firefox-targeted `/playback/sessions/:id/compat.mp4` fallback that transcodes the audio to AAC-LC through `ffmpeg` into a cached `faststart` MP4 with range support, so verify the client switched to the compat route, that the API still uses `-extension_picky 0` for AnimePahe’s disguised `.jpg` HLS segments, and that the cached compat file exists when users say the scrubber only knows about buffered ranges.
- `web/apps/client/components/video-player/` and `web/apps/client/components/video-player.tsx` are now the first files to check when playback fallback, keyboard shortcuts, subtitle-track defaults, progress pings, or source attachment behavior regress, because the big player component was split into a small shell over `playback-fallback.ts`, `use-playback-source.ts`, `use-playback-progress.ts`, and `use-keyboard-shortcuts.ts` and those focused helpers now hold the state-machine and effect-heavy logic.
- `web/packages/providers/src/base/provider-utils.ts` owns the shared HTML challenge detector used by HTTP providers. Check it first when Cloudflare-backed sites suddenly flip from working to `challenge-protected`, because some hosts now inject `/cdn-cgi/challenge-platform/scripts/jsd/main.js` or embed generic Turnstile widgets (`turnstile-recaptcha`) on otherwise normal pages, and those signals alone should not be treated as a blocking interstitial.
- `web/packages/providers/src/providers/javguru.ts` intentionally filters search results down to numeric `/<post-id>/<slug>/` permalinks. Use that file first when Javguru starts returning static pages like `advanced-search` or `jav-actress-list`, because the site mixes page links into search markup and broad anchor scraping will surface junk results.
- `web/packages/providers/src/providers/aniwave.ts` applies provider-side title relevance filtering on top of `/filter?keyword=...`. Check that file first when Aniwave starts returning a page full of loosely related cards, because the site search can include broad substring matches that look populated but are not actually relevant to the user query.
- `web/apps/client/app/(dashboard)/discover/page.tsx`, `web/apps/client/app/(dashboard)/anime/[providerId]/[externalAnimeId]/page.tsx`, and `web/apps/client/app/(dashboard)/watch/[libraryItemId]/[episodeId]/page.tsx` must encode IDs when building URLs and decode dynamic route params before reusing them in API calls. Check those routes when providers use slash-containing IDs like Javguru, otherwise params get split or double-encoded and users land on 404s / in-page `Not found`.
- `web/apps/client/app/(dashboard)/anime/[providerId]/[externalAnimeId]/page.tsx` is the first file to check when the detail screen's `Open on <Provider>` action points at the wrong upstream page or disappears, because provider-specific external URL mapping (including slash-safe ID encoding and Hanime first-episode fallback links) now lives beside the `Add to Library` action there.
- `web/apps/client/app/(dashboard)/discover/page.tsx` is also the first file to check when search cards lose thumbnails after provider dedupe, because grouped results now promote the richest source row (cover image, synopsis, year) to `primary` instead of always keeping the first provider hit, so regressions there can silently replace good posters with fallback placeholders.
- `web/apps/api/src/routes/catalog.ts` now owns the slash-safe catalog query endpoints and the same-origin `/media/proxy` route used by the web client for remote cover images. Check it together with `web/apps/client/lib/media.ts` when external IDs contain `/` or browsers block posters with `ERR_BLOCKED_BY_RESPONSE.NotSameOrigin`.
- `web/apps/api/src/routes/catalog.ts` and `web/apps/api/src/streaming/proxy.ts` are the first files to check when AnimeTake posters disappear in Discover/detail cards while title text still shows, because Cloudflare now serves `animetake.com.co/imgs/poster/...` only when the request includes AnimeTake `origin`/`referer`, and `getMediaProxyHeaders()` now injects those headers for `animetake.com.co` hosts before `/media/proxy` returns the bytes.
- `web/packages/providers/src/providers/javguru.ts` now prefers the shortcode button payloads over raw iframe scraping, and it promotes the Dood-style `STREAM DD` host into a direct MP4 before falling back to iframe embeds. Check it together with `web/apps/api/src/services/playback-service.ts` when Javguru playback falls back to a black screen or `Embeds disabled`, because stale sessions that cached old HTML/embed/HLS URLs also need to be treated as non-reusable, and Dood embed pages can include Turnstile scripts while still exposing a valid `pass_md5` player config.
- `web/apps/browser/src/extractors/aki-h.ts` is the first place to check when Aki-H playback breaks, because the provider only scrapes search/details/episodes over HTTP and the real stream comes from a referer-locked browser chain that eventually surfaces `aki-h.stream/file...` HLS playlists. If Aki-H starts returning 403s or blank playback, re-verify the `v.aki-h.com` play click, the `aki-h.stream/file|quality` response capture, and the `referer` header derived from `https://aki-h.stream/v/<id>`.
- `web/apps/browser/src/extractors/animepahe/index.ts`, `web/apps/browser/src/extractors/animepahe/api.ts`, `web/apps/browser/src/extractors/animepahe/details.ts`, `web/apps/browser/src/extractors/animepahe/playback.ts`, `web/apps/browser/src/extraction-service.ts`, and `web/apps/api/src/services/playback-service.ts` are the first files to check when AnimePahe search or playback starts failing, because the provider folder now splits the DDoS-Guard wait loop, page handoff parsing, and kwik/HLS manifest capture into focused modules while API-side stale-session reuse guards moved into `playback-service.ts`.
- `web/apps/api/src/routes/playback.ts` rewrites HLS playlists onto `/stream/:sessionId/...` paths, so never forward the upstream `content-length` or `content-range` headers when a playlist body is being rewritten. Check that route first when Firefox reports `NS_BINDING_ERROR` or players fail only on proxied HLS manifests, because stale upstream length headers can describe the pre-rewrite body and cause the browser to abort the response.
- `web/apps/browser/src/extractors/animeonsen/search-api.ts`, `web/apps/browser/src/extractors/animeonsen/page-fallback.ts`, `web/apps/browser/src/extractors/animeonsen/episodes.ts`, and `web/apps/browser/src/extractors/animeonsen/playback.ts` are the first files to check when AnimeOnsen search/details/playback break, because the provider folder now splits Meilisearch ranking, challenged-page fallback, episode payload parsing, and stream/subtitle mapping into separate modules around the same `api.animeonsen.xyz/v4` flow.
- `web/apps/browser/src/extractors/animetake/search.ts`, `web/apps/browser/src/extractors/animetake/catalog.ts`, `web/apps/browser/src/extractors/animetake/ajax.ts`, `web/apps/browser/src/extractors/animetake/playback.ts`, `web/apps/browser/src/extraction-service.ts`, `web/apps/api/src/services/catalog-service.ts`, and `web/apps/api/src/services/playback-service.ts` are the first files to check when AnimeTake search/details/playback break, because the provider folder now separates locator-based search submit/scrape logic, AJAX episode/server parsing, page fallback scraping, and playback resolution while the short AnimeTake catalog timeout plus cache-or-error fallback logic moved into the focused API services.
- `web/apps/browser/src/extractors/hentaihaven/search.ts`, `web/apps/browser/src/extractors/hentaihaven/catalog.ts`, `web/apps/browser/src/extractors/hentaihaven/player-api.ts`, `web/apps/browser/src/extractors/hentaihaven/playback.ts`, `web/apps/browser/src/browser/context-manager.ts`, `web/apps/browser/src/extraction-service.ts`, and `web/apps/api/src/services/playback-service.ts` are the first files to check when HentaiHaven search/details/playback regress, because the provider folder now splits search cards, episode/catalog scraping, `player-logic/api.php` request decoding, and stream mapping while the API-side stale-session reuse rules moved into `playback-service.ts`. HentaiHaven still depends on an ephemeral browser context with no cookie-jar reuse to avoid Cloudflare playback-state poisoning.
- `web/apps/api/src/routes/playback.ts` is the companion file to check when HentaiHaven reaches Relay's proxied HLS path but Firefox still spins with `NS_BINDING_ABORTED`, because the provider's `octopusmanifest.org` master playlists advertise subtitle renditions whose `URI`s are raw `.vtt` files rather than HLS subtitle playlists. Relay now strips those `TYPE=SUBTITLES` entries for HentaiHaven during HLS rewriting so `hls.js` uses Relay's separate subtitle tracks instead of trying to parse `WEBVTT` as `#EXTM3U`.
- `web/apps/api/src/routes/playback.ts`, `web/apps/api/src/streaming/proxy.ts`, and `web/apps/api/src/services/playback-service.ts` are the first files to check when HLS playback stalls on proxied `__upstream__` fragment requests even though the manifest itself loads, because stream proxy routing, content-type normalization, `~relay.*` alias stripping, and stale-session reuse policy now live in those three files instead of the old monolith.
- `web/apps/api/src/routes/playback.ts`, `web/apps/api/src/streaming/proxy.ts`, and `web/apps/api/src/services/playback-service.ts` are also the first files to check when LAN clients proxy API traffic through Next rewrites and HLS still freezes at `0:00`, because `__upstream__` path payloads now use `b64.` URL-safe encoding and the session-id-aware proxy resolution moved into the focused playback route/service split.
- `web/apps/api/src/routes/playback.ts`, `web/apps/api/src/streaming/proxy.ts`, `web/apps/api/src/services/playback-service.ts`, and `web/apps/browser/src/extractors/animeonsen/playback.ts` are the first files to check when DASH playback requests look like `/stream/stream_init*.m4s` or when the MPD loads but subrequests still escape to root paths like `/stream_init0.m4s`, because DASH BaseURL and segment rewriting now live in the stream helpers while session-base selection and upstream manifest capture are split between the playback service and the AnimeOnsen playback module.
- `web/apps/client/components/video-player.tsx` is the companion file to check when providers return a valid stream session but the browser still says no supported format was found, because DASH manifests now rely on `dashjs` there while HLS still uses `hls.js`. Check it together with `web/apps/api/src/routes/playback.ts` when a provider resolves to `.mpd`, because the client needs DASH attachment and the API stream proxy still has to serve relative manifest subrequests correctly.
- `web/apps/client/components/video-player.tsx` and `web/apps/client/lib/api-base-url.ts` are the first files to check when LAN devices can browse/login but any watch playback silently fails while host playback works, because `session.streamUrl` can still arrive as a loopback `PUBLIC_API_URL` target and the player now normalizes those URLs onto the active client API base (`/__relay_api` or configured API host) before attaching HLS/DASH/iframe sources. The same files also cover Android Firefox `Relay HLS playback error {}` stalls via compatibility MP4 fallback.
- `web/apps/client/components/video-player.tsx` should no longer force Android Firefox HLS sessions into compatibility MP4 at startup. It now starts on primary HLS for non-AnimePahe providers and uses one-time bidirectional fallback (`primary -> compat` on fatal HLS decode issues, `compat -> primary` when compat startup/errors stall at `0:00`). Check that file first when playback freezes without explicit network errors after recent fallback tuning.
- `web/apps/client/components/video-player/use-playback-source.ts` and `web/apps/client/components/video-player/playback-fallback.ts` are the first files to check when AnimePahe compat MP4 playback aborts around the 20 second mark on Firefox before any bytes arrive, because fresh `/playback/sessions/:id/compat.mp4` generation blocks until ffmpeg finishes writing the cached MP4 and AnimePahe cold-cache transcodes can take around a minute. Those client helpers now carry the provider-specific startup timeout that prevents the player from bailing back to primary HLS too early.
- `web/apps/client/hooks/use-playback-session-query.ts` and `web/apps/api/src/routes/playback.ts` are the next files to check when a watch page has been open long enough that `compat.mp4` or `/stream/:sessionId` suddenly starts returning `409 Playback session is not ready`, because ready playback sessions were previously left untouched until their 15-minute TTL elapsed. The client hook now rotates expired sessions automatically, and the compat route can still serve an already-cached MP4 after the original session has expired.
- `web/apps/client/lib/api-base-url.ts` and `web/apps/client/components/video-player/use-playback-source.ts` are the next files to check when AnimePahe compat MP4 returns `500` only through `http://localhost:3000/__relay_api/...` after roughly 40 seconds while the API eventually finishes transcoding, because Next's local rewrite proxy can time out long-running compat requests before Fastify sends the first byte. The player now derives the compat URL from the resolved primary stream origin so loopback browsers hit `http://localhost:4000` directly while LAN browsers keep the `__relay_api` prefix.
- `web/apps/client/components/video-player/playback-fallback.ts` and `web/apps/client/components/video-player/use-playback-source.ts` are also the first files to check when AnimePahe compat appears to "hang" too long before the successful retry, because Firefox starts in compat mode for AnimePahe and the player now uses a shorter timeout for the first compat attempt but a longer one for the retry after the primary HLS failure. Tune those two paths together so the first attempt fails fast enough to feel responsive without cutting off the retry that finishes after the transcode cache is warm.
- `web/apps/client/app/globals.css` is the first file to check when the watch page starts horizontally scrolling on phones or the bottom mobile nav/footer looks offset while an episode is open, because the watch layout and `.player-frame` sizing live there and the fix depends on keeping `.watch-main` / `.watch-player-shell` shrinkable (`min-width: 0`) so the player can stay inside the viewport.
- `web/apps/api/src/lib/subtitles.ts` and `web/apps/client/app/globals.css` are the first files to check when AnimeOnsen subtitles load but look out of order or visually wrong, because the provider serves ASS without file extensions and Relay now approximates that by converting ASS `\pos(x,y)` tags into WebVTT cue placement while styling browser `::cue` output to remove the default black boxes. Re-verify the ASS-to-VTT placement math and cue styling before assuming the subtitle URLs themselves changed.
- `web/packages/providers/src/providers/aniwave.ts` should prefer MyCloud/Cloudora over Vidplay from `/ajax/server/list`, and `web/apps/api/src/services/playback-service.ts` is the companion file to check when old sessions keep redirecting to dead `shipimagesbolt.online/embed-1/...` pages. Use those two files first when Aniwave says a file was deleted while the same episode still works on-site, because Vidplay often maps to dead `embed-1` tokens while MyCloud/Cloudora still return usable embeds, and prefetching the chosen embed URL can invalidate the token before the client opens it.
- `web/apps/api/src/routes/catalog.ts` is also the first file to check when Discover provider search streams fail in-browser with CORS errors even though normal JSON routes work, because `/catalog/search/stream` uses `reply.hijack()` for NDJSON and must set `Access-Control-Allow-Origin`/`Access-Control-Allow-Credentials` directly on `reply.raw`; the legacy `GET /stream?query=...` alias has been removed, so older clients must migrate to the canonical route.
- `web/apps/client/components/app-shell.tsx` and `web/apps/client/app/(dashboard)/settings/page.tsx` are the first files to check when signed-out users on narrow screens cannot find Login or Settings appears stuck on `Loading settings...`, because mobile nav now swaps its fourth slot between `Settings` (signed-in) and `Login` (signed-out) and Settings disables `/me` retries to show an immediate unauthenticated CTA to `/login`.
- `web/apps/client/app/(dashboard)/settings/page.tsx` and `web/apps/client/app/globals.css` are the first files to check when Provider controls disappear on narrow screens, because Settings now exposes mobile-only `General` / `Providers` tabs and renders provider enable/priority controls inline under the `Providers` tab instead of relying on the hidden sidebar route links.
- `web/apps/client/hooks/use-route-access.ts`, `web/apps/client/hooks/use-session-query.ts`, `web/apps/client/components/auth-required-state.tsx`, and `web/apps/client/app/page.tsx` are now the first files to check when protected routes hang on loading spinners, signed-out users land on raw request errors, or `/` stops resolving cleanly to Login vs Discover, because the redesign centralized auth-aware route gating there instead of letting each page fail independently.
- `web/packages/contracts/src/imports.ts`, `web/apps/api/src/routes/imports.ts`, `web/apps/api/src/services/import-service.ts`, `web/apps/api/src/repositories/import-repository.ts`, `web/apps/client/hooks/use-imports-query.ts`, and `web/apps/client/app/(dashboard)/settings/page.tsx` are the first files to check when Account > Data stops showing import history, because the web redesign now depends on the new `GET /imports` listing flow rather than only one-off job polling.
- `web/apps/client/app/(dashboard)/library/page.tsx`, `web/apps/client/app/(dashboard)/settings/page.tsx`, and `web/apps/client/lib/api.ts` are the first files to check when library layout/sort/category visibility stops persisting or tracker/import panels regress into generic failures, because the redesign now round-trips those preferences through `/me/preferences` and uses shared `ApiError` helpers to separate unauthenticated, admin-only, and general error states.
- `web/apps/worker/src/jobs/` and `web/packages/contracts/src/worker.ts` are now the first files to check when queue names drift or worker result payloads stop matching expectations, because the worker’s queue handlers were split out of `web/apps/worker/src/index.ts` and now validate their stub result payloads against the shared worker contract module.

## Sub Agents

Use sub-agents where appropriate to break down complex changes into manageable pieces, and to allow for more focused implementation and testing. For example, if implementing a new feature that requires both backend and frontend changes, you might create separate sub-agents for each layer of the stack, but before then use an exploring agent (or multiple) to get context on the codebase and research the best approaches for the feature, outline the specific steps needed for implementation into a final exec plan, and spin up task subagents that handle the implementation. This allows for more efficient development and testing, as each sub-agent can focus on a specific aspect of the implementation, and can be tested independently before being integrated into the larger codebase.

## Final Output

When asking the user to verify implemented changes, output a checklist they can fill to make sure everything works as intended. Describe what they should see, how it should work, and what they need to manually test. The user will then fill in the checklist and provide feedback on any issues they encounter, which can be used to further refine the implementation.

If the user asked for multiple changes and only some were implemented, make sure to clearly indicate which ones were completed, which ones were not fully realized, and which ones are still pending. For example:

```txt
- [x] Implement app scaffold (completed with basic layout and navigation)
- [~] Implement feature A (stub implementation completed)
- [ ] Implement feature B (pending due to X reason)
```

Include a commit message after each implementation or fix, following the Conventional Commits specifications. If it's a large change, follow this format:

```txt
feat(update): add startup update prompt choices and sectioned changelog pipeline
- feat(update): gate startup updates behind user choice (Yes/No/Remind Later)
- feat(update): persist per-release prompt decisions (ignore until newer, 24h remind-later)
- refactor(update): split updater flow into eligibility check and install phases
- feat(update): parse GitHub release body into sectioned changelog blocks for in-app prompt
- test(update): add updater decision/state-store/changelog parser coverage
- feat(ci): generate release notes sections from commit metadata and publish via body_path
- feat(ci): support multi-section changelog from Conventional Commit lines in commit body
- fix(navigation): clamp bottom navbar sizing to prevent tiny rendering on some phones
- fix(navigation): make top-level tab swipe detection more reliable in Explore
- fix(search): move Explore apply+navigate to app scope to prevent canceled loads on slower devices
- docs(readme): document updater prompt behavior and changelog contract
```
